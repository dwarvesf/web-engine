---
template: work-template
title: 'Project Title'
description: 'Brief description of the project'
image: '/images/work/project-hero.jpg'
category: 'Web Development'
tags: ['React', 'Next.js', 'TypeScript']
client: 'Client Name'
date: '2024-01-15'
url: 'https://example.com'
---

<!-- This template will automatically use theme components via the MDX provider -->

<Hero
  title={frontmatter.title}
  subtitle={frontmatter.category}
  description={frontmatter.description}
  backgroundImage={frontmatter.image}
  ctaText="View Live Project"
  ctaLink={frontmatter.url}
/>

<section className="py-16 bg-background">
  <div className="max-w-4xl mx-auto px-4">
    <div className="grid md:grid-cols-3 gap-8 mb-12">
      <div>
        <h3 className="text-sm uppercase tracking-wider text-primary font-medium mb-2">Client</h3>
        <p className="text-foreground font-semibold">{frontmatter.client}</p>
      </div>
      <div>
        <h3 className="text-sm uppercase tracking-wider text-primary font-medium mb-2">Date</h3>
        <p className="text-foreground font-semibold">{new Date(frontmatter.date).toLocaleDateString()}</p>
      </div>
      <div>
        <h3 className="text-sm uppercase tracking-wider text-primary font-medium mb-2">Technologies</h3>
        <div className="flex flex-wrap gap-2">
          {frontmatter.tags.map((tag, index) => (
            <span key={index} className="bg-tag text-tag-foreground px-2 py-1 rounded text-xs">
              {tag}
            </span>
          ))}
        </div>
      </div>
    </div>

    <div className="prose prose-lg max-w-none">
      ## Project Overview

      Describe the project goals, challenges, and approach. This template provides a structured layout for showcasing work projects with consistent styling and components.

      ## Key Features

      - Feature 1: Description of the first key feature
      - Feature 2: Description of the second key feature
      - Feature 3: Description of the third key feature

      ## Technical Implementation

      Detail the technical aspects, architecture decisions, and implementation details.

      ## Results & Impact

      Highlight the outcomes, metrics, and business impact of the project.
    </div>

  </div>
</section>

<section className="bg-secondary-background py-16">
  <div className="mx-auto max-w-6xl px-4">
    <h2 className="text-foreground mb-12 text-center text-3xl font-bold">
      Related Work
    </h2>
    <div className="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
      <WorkCard
        title="Project Alpha"
        description="Brief description of related project"
        image="/images/work/project-alpha.jpg"
        category="Web Development"
        tags={['React', 'Node.js']}
        href="/work/project-alpha"
      />
      <WorkCard
        title="Project Beta"
        description="Brief description of related project"
        image="/images/work/project-beta.jpg"
        category="Mobile App"
        tags={['React Native', 'TypeScript']}
        href="/work/project-beta"
      />
      <WorkCard
        title="Project Gamma"
        description="Brief description of related project"
        image="/images/work/project-gamma.jpg"
        category="Design System"
        tags={['Figma', 'Storybook']}
        href="/work/project-gamma"
      />
    </div>
  </div>
</section>
